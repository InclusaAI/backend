// Re-exports this service's own generated Prisma client.
//
// Import from here rather than '@prisma/client': under pnpm that bare
// specifier resolves to a directory shared with the other services, whose
// contents are whichever schema ran `prisma generate` last.
export * from "../../generated/prisma";
